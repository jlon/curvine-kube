apiVersion: v1
kind: Pod
metadata:
  name: worker-template-invalid
spec:
  volumes:
    - name: data-dir-0
      hostPath:
        path: /tmp/curvine-data
        type: DirectoryOrCreate
  containers:
    - name: cv-worker
      volumeMounts:
        # This mountPath is WRONG - should be /data/mem
        # This should cause validation to fail
        - name: data-dir-0
          mountPath: /wrong/path/data

