apiVersion: v1
kind: Pod
metadata:
  name: master-template-invalid
spec:
  volumes:
    - name: meta-data
      hostPath:
        path: /tmp/curvine-meta
        type: DirectoryOrCreate
  containers:
    - name: cv-master
      volumeMounts:
        # This mountPath is WRONG - should be /app/curvine/testing/meta
        # This should cause validation to fail
        - name: meta-data
          mountPath: /wrong/path/meta

